server:
  port: 8080
  servlet:
    context-path: /start
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my healthcheck-in app
    username: root
    password: root

#mybatis:
#  mapper-locations: classpath*:mapper/*.xml
#  type-aliases-package: edu.vojago.backend_healthcheckinsystem.mapper
